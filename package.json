{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "prisma-generate": "turbo run db:generate",
    "prisma-push": "turbo run db:push"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "turbo": "^1.10.3"
  },
  "name": "turborepo-react-express",
  "packageManager": "npm@9.5.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
